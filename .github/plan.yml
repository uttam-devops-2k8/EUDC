name: Terraform Plan

on:
  pull_request:

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  terraform_plan:
    uses: uttam-devops-2k8/EUDC/.github/workflows/create-tfsa.yml@main
    secrets: inherit
    with:
      runner: ubuntu-latest
      terraform_version: 1.14.3
